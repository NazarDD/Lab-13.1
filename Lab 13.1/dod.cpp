#include "var.h"
#include "sum.h"
#include <iostream>
#include <cmath>
#include "dod.h"
using namespace std;
using namespace nsVar;
using namespace nsSum;
using namespace nsDod;

void nsDod::dod() {
    a *= ((-1) * pow(x, 2 * n + 1)) / (2 * n + 1);
}